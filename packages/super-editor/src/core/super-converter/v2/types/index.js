/**
 * Represents a node in the SuperDoc document structure.
 * @typedef {Object} SuperDocNode
 */

/**
 * Represents a node in the Open XML document structure.
 * @typedef {Object} OpenXmlNode
 */

export {};
